'use client';

import { useEffect, useMemo, useState } from 'react';

const today = new Date().toISOString().slice(0, 10);
const GPT_URL = 'https://chatgpt.com/g/g-6a03e60a20948191b57eb98f7cbf4672-hai-yue-tu-di-ping-gu-diao-yan-zhu-shou';
const STORAGE_KEY = 'hiyes-land-evaluation-draft-v2';
const emptyForm = { client: '', researchDate: today, landNumber: '', reportText: '' };
const createReportId = () => `hy-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;

function fileName(form) { return `${form.client || '土地評估'}_${form.landNumber || '地號'}_${form.researchDate || today}`.replace(/[\\/:*?"<>|]/g, '-'); }
function buildPrompt(form, reportId) { return `請直接完成土地評估報告，並在完成後呼叫 submitReport 動作回傳結果。\n\n配合業主：${form.client || '＿＿建設'}\n調研日期：${form.researchDate || today}\n目標地號：${form.landNumber || '＿＿地號'}\nreport_id：${reportId}\n\n重要規則：\n1. 不要只回覆調研過程，不要只列待複核事項。\n2. 請在合理時間內直接產出完整報告；公開資料不足時，欄位內填「依公開資料初判／待複核」，不要因此停止。\n3. 內容要適合業主閱讀，不要把過多查證過程塞進摘要；摘要只講重劃區、分區、基地坪數、建蔽率、容積率、臨路、學區里別與建議成交價格。\n4. 價格預判只提供「建議成交價格」、「建議總價帶」、「車位建議開價」，不要出現建議表價、首波成交價。\n5. 建議產品只寫兩房、三房，不要寫四房。\n6. 報告必須使用以下固定欄位名稱，方便土地評估系統解析：\n配合業主：\n調研時間：\n標的位置：\n標的地號：\n土地分區：\n基地面積：\n法定建蔽率：\n法定容積率：\n臨路條件：\n土地售價：\n學區：\n里別：\n基地現況：\n東向：\n南向：\n西向：\n北向：\n交通動線：\n生活機能：\n公共建設：\n區域銷況：\n個案參考：\n價格預判：\n建議產品：\n優勢一：\n優勢二：\n優勢三：\n劣勢一：\n劣勢二：\n劣勢三：\n初步結論：\n資料來源：\n待複核事項：\n\n7. 完成完整報告後，請務必呼叫 submitReport 動作，參數如下：\nreport_id：${reportId}\nclient：${form.client || ''}\nland_number：${form.landNumber || ''}\nresearch_date：${form.researchDate || today}\nreport_text：完整土地評估報告全文\n\n8. submitReport 成功後，請只回覆：「報告已送回土地評估系統，請回到原系統查看業主閱讀版報告。」`; }
function buildShortPrompt(form, reportId) { return `請依海悅土地評估格式完成報告並呼叫 submitReport 回傳。配合業主：${form.client || '＿＿建設'}；調研日期：${form.researchDate || today}；目標地號：${form.landNumber || '＿＿地號'}；report_id：${reportId}。價格只留建議成交價格、總價帶、車位開價；產品只寫兩房、三房。完成後請送回土地評估系統。`; }
function gptLink(shortPrompt) { return `${GPT_URL}?q=${encodeURIComponent(shortPrompt)}`; }
function normalize(text) { return String(text || '').replace(/\r\n/g, '\n').replace(/\r/g, '\n').replace(/\n{3,}/g, '\n\n').trim(); }
function section(text, label) { const labels = ['配合業主','調研時間','標的位置','標的地號','土地分區','基地面積','法定建蔽率','法定容積率','臨路條件','土地售價','學區','里別','基地現況','東向','南向','西向','北向','交通動線','生活機能','公共建設','區域銷況','個案參考','價格預判','建議產品','優勢一','優勢二','優勢三','劣勢一','劣勢二','劣勢三','初步結論','資料來源','待複核事項']; const esc = (s) => s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'); const all = labels.map(esc).join('|'); const pattern = new RegExp(`(?:^|\\n)\\s*${esc(label)}\\s*[：:]\\s*([\\s\\S]*?)(?=\\n\\s*(?:${all})\\s*[：:]|$)`, 'i'); return normalize(text).match(pattern)?.[1]?.trim() || '依公開資料初判／待複核'; }
function compactText(value) { return String(value || '').replace(/\s+/g, ' ').trim(); }
function sentenceMatch(text, keywords) { const parts = compactText(text).split(/[。；;\n]/).map((s) => s.trim()).filter(Boolean); return parts.find((s) => keywords.some((k) => s.includes(k))) || ''; }
function extractPing(text) { const matches = compactText(text).match(/約?\d[\d,\.]*\s*坪/g); return matches?.[0] || compactText(text).match(/\d[\d,\.]*\s*坪/g)?.[0] || compactText(text); }
function extractRedevelopmentArea(text) { const value = compactText(text); const match = value.match(/([\u4e00-\u9fa5A-Za-z0-9]+重劃區)/); return match?.[1] || sentenceMatch(value, ['重劃區']) || value; }
function extractZone(text) { const value = compactText(text); const match = value.match(/(住宅區|商業區|工業區|乙種工業區|農業區|保存區|特定專用區|都市發展用地[^，。；\s]*)/); return match?.[1] || value; }
function extractRoad(text) { const value = compactText(text); const front = value.match(/[一二三四雙單]面臨路|\d面臨路|三面臨路|雙面臨路|單面臨路/)?.[0]; const roads = Array.from(new Set((value.match(/[\u4e00-\u9fa5A-Za-z0-9]+(?:路|街|巷|大道|段)/g) || []).slice(0, 6))); return [front, roads.length ? `臨${roads.join('、')}` : ''].filter(Boolean).join('，') || value; }
function cleanPriceText(text) { return compactText(text).replace(/[^。；;\n]*(建議表價|首波成交價|表價)[^。；;\n]*[。；;]?/g, '').trim(); }
function extractPricing(text) { const clean = cleanPriceText(text); return { deal: sentenceMatch(clean, ['建議成交價格', '建議成交均價', '成交價格', '成交均價', '建議成交']) || clean, total: sentenceMatch(clean, ['總價帶', '總價區間', '總價']) || '依產品坪數與成交單價回推，需待產品規劃確認。', parking: sentenceMatch(clean, ['車位']) || '車位建議開價待周邊競案車位行情複核。' }; }
function splitCases(text) { const value = normalize(text); if (!value || value.includes('待複核')) return [{ title: '競案參考', body: value }]; const parts = value.split(/\n(?=\s*(?:\d+[\.、]|[一二三四五六七八九十]+、|案名|個案|競案))/g).map((s) => s.trim()).filter(Boolean); return (parts.length ? parts : [value]).slice(0, 8).map((body, i) => { const first = body.split('\n')[0].replace(/^\d+[\.、]\s*/, '').replace(/^[一二三四五六七八九十]+、\s*/, '').replace(/^案名[：:]/, '').trim(); const title = first.split(/[：:，,。]/)[0] || `競案 ${i + 1}`; return { title, body }; }); }
function extractProduct(text) { const value = normalize(text).replace(/[^。；;\n]*四房[^。；;\n]*[。；;]?/g, ''); const two = sentenceMatch(value, ['兩房', '2房']); const three = sentenceMatch(value, ['三房', '3房']); return { two: two || '兩房坪數建議待產品規劃確認。', three: three || '三房坪數建議待產品規劃確認。' }; }
function P({ children }) { return <p>{children}</p>; }
function Card({ title, children, accent = false, className = '' }) { return <section className={`info-card readable-card ${accent ? 'accent' : ''} ${className}`}><h3>{title}</h3>{children}</section>; }
function Metric({ label, value }) { return <div className="metric-card readable-metric"><small>{label}</small><strong>{String(value || '待複核')}</strong></div>; }
function Heading({ index, title, subtitle }) { return <div className="section-heading readable-heading"><span>{index}</span><div><h2>{title}</h2>{subtitle && <p>{subtitle}</p>}</div></div>; }
function KeyValue({ label, value }) { return <div className="key-value"><span>{label}</span><strong>{value}</strong></div>; }
function Report({ text }) { const get = (label) => section(text, label); const location = get('標的位置'); const zone = extractZone(get('土地分區')); const area = extractPing(get('基地面積')); const coverage = get('法定建蔽率'); const far = get('法定容積率'); const road = extractRoad(get('臨路條件')); const pricing = extractPricing(get('價格預判')); const cases = splitCases(get('個案參考')); const product = extractProduct(get('建議產品')); return <article className="card-report readable-report"><div className="report-brand-row"><div className="report-brand-lockup"><div className="report-brand-mark">H</div><div><div className="report-brand-title">HIYES</div><div className="report-brand-subtitle">海悅廣告｜土地評估系統</div></div></div><div className="report-brand-side"><span>土地開發初評</span><span>OWNER REPORT</span></div></div><section className="report-section cover-section"><Heading index="01" title="標的摘要總覽" subtitle="先看基地所在生活圈、土地條件與可銷售價格方向。" /><div className="summary-grid owner-summary compact-summary"><Metric label="所在重劃區 / 生活圈" value={extractRedevelopmentArea(location)} /><Metric label="基地面積" value={area} /><Metric label="土地分區" value={zone} /><Metric label="建蔽率 / 容積率" value={`住宅區建蔽率 ${coverage}｜住宅區容積率 ${far}`} /><Metric label="臨路條件" value={road} /><Metric label="建議成交價格" value={pricing.deal} /></div></section><section className="report-section"><Heading index="02" title="基地面積與土地分區" subtitle="此段只保留業主判斷開發價值需要的關鍵條件。" /><div className="owner-facts tight-facts"><Card title="基地面積" accent><div className="big-number">{area}</div></Card><Card title="土地使用與容積條件" className="compact-card"><KeyValue label="使用分區" value={zone} /><KeyValue label="住宅區建蔽率" value={coverage} /><KeyValue label="住宅區容積率" value={far} /></Card></div></section><section className="report-section"><Heading index="03" title="地段、臨路與學區資訊" /><div className="owner-facts"><Card title="標的位置"><P>{location}</P></Card><Card title="臨路條件"><P>{road}</P></Card><Card title="學區與里別"><P>學區：{get('學區')}</P><P>里別：{get('里別')}</P></Card><Card title="標的地號"><P>{get('標的地號')}</P></Card></div></section><section className="report-section"><Heading index="04" title="區域銷況與競案參考" subtitle="競案以一案一卡呈現，方便業主直接比較。" /><Card title="區域銷況" accent><P>{get('區域銷況')}</P></Card><div className="owner-case-list case-card-grid">{cases.map((item, index) => <Card key={`${item.title}-${index}`} title={item.title || `競案 ${index + 1}`} className="case-card"><P>{item.body}</P></Card>)}</div></section><section className="report-section"><Heading index="05" title="價格預判" subtitle="僅保留成交判斷，不顯示建議表價或首波成交價。" /><div className="owner-facts tight-facts"><Card title="建議成交價格" accent><P>{pricing.deal}</P></Card><Card title="建議總價帶"><P>{pricing.total}</P></Card><Card title="車位建議開價"><P>{pricing.parking}</P></Card></div></section><section className="report-section"><Heading index="06" title="產品建議" subtitle="本系統聚焦兩房、三房產品，不列四房。" /><div className="product-grid"><Card title="兩房產品" accent><P><strong>兩房：</strong>{product.two}</P></Card><Card title="三房產品" accent><P><strong>三房：</strong>{product.three}</P></Card></div></section><section className="report-section"><Heading index="07" title="基地四向現況與環境條件" /><div className="owner-facts"><Card title="基地四向現況"><div className="direction-list"><div><strong>東向</strong><p>{get('東向')}</p></div><div><strong>南向</strong><p>{get('南向')}</p></div><div><strong>西向</strong><p>{get('西向')}</p></div><div><strong>北向</strong><p>{get('北向')}</p></div></div></Card><Card title="交通動線"><P>{get('交通動線')}</P></Card><Card title="生活機能"><P>{get('生活機能')}</P></Card><Card title="公共建設"><P>{get('公共建設')}</P></Card></div></section><section className="report-section"><Heading index="08" title="綜合評估" /><div className="swot-grid owner-swot"><div className="swot-card strengths"><h3>3 點優勢</h3><P><strong>優勢 1</strong>{get('優勢一')}</P><P><strong>優勢 2</strong>{get('優勢二')}</P><P><strong>優勢 3</strong>{get('優勢三')}</P></div><div className="swot-card weaknesses"><h3>3 點劣勢</h3><P><strong>劣勢 1</strong>{get('劣勢一')}</P><P><strong>劣勢 2</strong>{get('劣勢二')}</P><P><strong>劣勢 3</strong>{get('劣勢三')}</P></div></div></section><section className="report-section"><Heading index="09" title="初步結論與待複核事項" /><div className="owner-facts"><Card title="初步結論" accent><P>{get('初步結論')}</P></Card><Card title="資料來源與待複核"><P>資料來源：{get('資料來源')}</P><P>待複核事項：{get('待複核事項')}</P></Card></div></section><div className="report-credit">海悅機構｜海宇國際 戴異軒 製</div></article>; }

export default function Page() {
  const [form, setForm] = useState(emptyForm); const [reportId, setReportId] = useState(createReportId); const [waiting, setWaiting] = useState(false); const [copied, setCopied] = useState(false); const [viewMode, setViewMode] = useState('card'); const [syncMessage, setSyncMessage] = useState('尚未開始等待 GPT 回傳。'); const [lastLink, setLastLink] = useState(''); const [loaded, setLoaded] = useState(false); const hasReport = form.reportText.trim().length > 0; const canOpen = form.client.trim() && form.landNumber.trim(); const prompt = useMemo(() => buildPrompt(form, reportId), [form, reportId]); const shortPrompt = useMemo(() => buildShortPrompt(form, reportId), [form, reportId]); const setField = (key, value) => setForm((old) => ({ ...old, [key]: value }));
  useEffect(() => { try { const saved = JSON.parse(localStorage.getItem(STORAGE_KEY) || '{}'); if (saved.form) setForm({ ...emptyForm, ...saved.form }); if (saved.reportId) setReportId(saved.reportId); if (saved.waiting) setWaiting(saved.waiting); if (saved.lastLink) setLastLink(saved.lastLink); if (saved.syncMessage) setSyncMessage(saved.syncMessage); } catch {} setLoaded(true); }, []); useEffect(() => { if (!loaded) return; localStorage.setItem(STORAGE_KEY, JSON.stringify({ form, reportId, waiting, lastLink, syncMessage })); }, [loaded, form, reportId, waiting, lastLink, syncMessage]);
  async function checkReturnedReport(silent = false, overrideId) { const targetId = String(overrideId || reportId || '').trim(); if (!targetId) return; if (!silent) setSyncMessage(`正在查詢 report_id：${targetId}`); try { const res = await fetch(`/api/reports/${encodeURIComponent(targetId)}?t=${Date.now()}`, { cache: 'no-store' }); const data = await res.json().catch(() => ({})); if (res.status === 404) { if (!silent) setSyncMessage(`尚未找到 report_id：${targetId}。若 GPT 剛送出，請等 5 秒再試。`); return; } if (!res.ok) { setSyncMessage(`查詢失敗：${data.error || res.status}`); return; } if (data?.report?.report_text) { setReportId(targetId); setForm((old) => ({ ...old, client: data.report.client || old.client, landNumber: data.report.land_number || old.landNumber, researchDate: data.report.research_date || old.researchDate, reportText: data.report.report_text })); setWaiting(false); setViewMode('card'); setSyncMessage(`已成功載入 report_id：${targetId}`); } } catch (error) { if (!silent) setSyncMessage(`檢查回傳時發生錯誤：${error.message || '未知錯誤'}`); } }
  useEffect(() => { if (!waiting || hasReport) return; const timer = setInterval(() => checkReturnedReport(true), 5000); return () => clearInterval(timer); }, [waiting, hasReport, reportId]); useEffect(() => { const onFocus = () => { if (waiting && !hasReport) checkReturnedReport(true); }; window.addEventListener('focus', onFocus); return () => window.removeEventListener('focus', onFocus); }, [waiting, hasReport, reportId]);
  async function copyPrompt() { await navigator.clipboard.writeText(prompt); setCopied(true); setTimeout(() => setCopied(false), 2200); }
  async function openGpt() { if (!canOpen) { alert('請先填寫配合業主與目標地號。'); return; } const link = gptLink(shortPrompt); setLastLink(link); try { await navigator.clipboard.writeText(prompt); setCopied(true); setTimeout(() => setCopied(false), 2200); } catch {} setWaiting(true); setSyncMessage(`已開啟海悅土地評估助手，並嘗試自動帶入短提示詞；完整提示詞也已複製。若輸入框未自動帶入，請按 Command + V。report_id：${reportId}`); window.open(link, '_blank'); }
  function reset() { const nextId = createReportId(); setForm(emptyForm); setReportId(nextId); setWaiting(false); setSyncMessage('已建立新的空白評估。'); setLastLink(''); localStorage.removeItem(STORAGE_KEY); } function printPdf() { document.title = fileName(form); window.print(); } async function copyReport() { if (hasReport) { await navigator.clipboard.writeText(form.reportText); alert('已複製完整報告。'); } }
  return <main className="app-shell"><section className="hero-panel"><div className="hero-topbar"><div className="brand-lockup"><div className="brand-mark">H</div><div className="brand-copy"><div className="brand-copy-top">HIYES</div><div className="brand-copy-bottom">海悅廣告｜土地評估系統</div></div></div><div className="public-chip">Owner Report • GPT + PDF</div></div><div className="eyebrow">Hiyes Advertising Land Intelligence</div><div className="hero-content"><div><h1 className="app-title">土地評估業主閱讀版報告</h1><p className="app-subtitle">輸入業主與地號後，自動開啟海悅土地評估助手；完成後透過 Action 回傳並生成報告。</p></div><div className="toolbar hero-actions"><button className="btn ghost" onClick={reset}>新評估 / 清空</button><button className="btn primary" onClick={printPdf} disabled={!hasReport}>輸出 PDF</button></div></div><div className="status-row"><span>自動保存 report_id</span><span>短提示詞自動帶入</span><span>完整提示詞自動複製</span><span>PDF 可輸出</span></div></section><section className="workflow-grid"><section className="panel input-panel"><div className="panel-header compact"><p className="eyebrow small">Step 1</p><h2>輸入資料後進入 GPT 自動調研</h2><p className="muted">按自動調研後，系統會嘗試自動帶入短提示詞，並同步複製完整提示詞。</p></div><div className="panel-body simple-form"><label className="field"><span>配合業主</span><input value={form.client} onChange={(e) => setField('client', e.target.value)} placeholder="例如：弘峻建設" /></label><label className="field"><span>調研日期</span><input type="date" value={form.researchDate} onChange={(e) => setField('researchDate', e.target.value)} /></label><label className="field"><span>目標地號</span><textarea rows={4} value={form.landNumber} onChange={(e) => setField('landNumber', e.target.value)} placeholder="例如：桃園市中壢區中運段156、157、160地號" /></label><div className="sync-box"><strong>回傳編號 report_id</strong><input value={reportId} onChange={(e) => setReportId(e.target.value.trim())} style={{ width:'100%', border:'1px solid var(--line)', borderRadius:12, padding:10, fontWeight:800, color:'#0d5f87' }} /><p>{syncMessage}</p>{lastLink && <a className="fallback-link" href={lastLink} target="_blank" rel="noreferrer">開啟海悅土地評估助手</a>}</div><div className="action-card"><div><strong>操作流程</strong><p>按自動調研 → 嘗試自動帶入短提示詞 → 若未帶入按 Command + V → GPT 完成報告 → submitReport 回傳 → 本頁生成業主閱讀版。</p></div><div className="toolbar"><button className="btn" onClick={copyPrompt}>{copied ? '提示詞已複製' : '複製完整提示詞'}</button><button className="btn primary" onClick={openGpt} disabled={!canOpen}>自動調研</button><button className="btn" onClick={() => checkReturnedReport(false)}>檢查 / 載入回傳</button></div></div></div></section><section className="panel preview-panel"><div className="panel-header preview-header"><div><p className="eyebrow small">Step 2</p><h2>生成業主閱讀版報告</h2><p className="muted">若 GPT Action 已回傳會自動生成；也可手動貼上完整報告。</p></div><div className="toolbar"><button className="btn" onClick={copyReport} disabled={!hasReport}>複製全文</button><button className="btn primary" onClick={printPdf} disabled={!hasReport}>PDF</button><button className="btn locked" onClick={() => alert('Word 輸出僅限戴異軒本人使用。')}>Word</button></div></div><div className="paste-area no-print"><label className="field"><span>貼上 GPT 產出的完整土地評估報告</span><textarea rows={10} value={form.reportText} onChange={(e) => setField('reportText', e.target.value)} placeholder="若 Action 沒自動回傳，也可以手動貼上 GPT 報告。" /></label></div><div className="report-tabs no-print"><button className={viewMode === 'card' ? 'active' : ''} onClick={() => setViewMode('card')}>業主閱讀版</button><button className={viewMode === 'raw' ? 'active' : ''} onClick={() => setViewMode('raw')}>原文版</button></div>{hasReport ? (viewMode === 'card' ? <Report text={form.reportText} /> : <article className="report-paper raw-paper"><div className="report-brand-row"><div className="report-brand-title">HIYES｜原文版</div></div><pre className="text-report">{form.reportText}</pre><div className="report-credit">海悅機構｜海宇國際 戴異軒 製</div></article>) : <article className="report-paper"><pre className="text-report">尚未收到土地評估報告。\n\n請先輸入配合業主、調研日期與目標地號，點擊「自動調研」。若 GPT Action 正常執行，完成後會自動回傳到此處。\n\n如果已經回傳但畫面沒有更新，請確認左側 report_id 是否正確，然後按「檢查 / 載入回傳」。</pre><div className="report-credit">海悅機構｜海宇國際 戴異軒 製</div></article>}</section></section><footer className="designer-credit"><span>Designed by</span><strong>海悅機構｜海宇國際 戴異軒 製</strong></footer></main>;
}
